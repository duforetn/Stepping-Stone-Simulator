Welcome Stepping-stone-simulator

1. To compile type:
	$> make lapack
	$> make

2. To run the simulator
	$> ./ForwardIBD
        //will display the manual if compilation went OK

3. To clean your folder from executable files:
	$> make clean
	// removes the files in obj and PCAdapt
	$> make realclean
	// removes the files in obj, obj_lapack, and PCAdapt

version 08/23/14:
Ongoing project. Simulation of stepping stone in 1 and 2 dimensions are tested.
Expansion in 1 dimension is tested
Expansion in 2 dimensions implemented: The expansion occurs linearly along the second dimension, row by row.
